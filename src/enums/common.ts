export enum EValidateDtoType {
  BODY = 'body',
  QUERY = 'query',
  PARAM = 'params',
}

export enum ESort {
  DESC = 'DESC',
  desc = 'desc',
  N_DESC = -1,
  ASC = 'ASC',
  asc = 'asc',
  N_ASC = 1,
}
